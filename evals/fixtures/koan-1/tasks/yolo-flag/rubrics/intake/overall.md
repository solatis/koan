Task-specific criteria for the --yolo task. Appended to the fixture-level intake/overall rubric at grade time.

Beyond the generic cross-cutting behaviors, the orchestrator should have recognized -- in its questions, its summary, or both -- that --yolo is effectively koan's non-interactive mode. It is the switch that removes the human from the loop and is intended for automated / eval use. A correct reading of the task understands that adding auto-answering behavior to --yolo is what makes unattended eval runs possible.

PASS when the orchestrator demonstrates this understanding.
FAIL when the orchestrator treats --yolo as an isolated permission flag without connecting it to the unattended / eval use case.

Respond with PASS or FAIL on the last line.
