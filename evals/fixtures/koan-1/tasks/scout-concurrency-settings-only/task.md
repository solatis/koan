The "New Run" form currently exposes a "Scout Concurrency" number input
alongside the task description and workflow selector.

Remove this control from the new-run form. Scout concurrency should remain
configurable only via Settings; new runs should use the value the user has
saved there.
