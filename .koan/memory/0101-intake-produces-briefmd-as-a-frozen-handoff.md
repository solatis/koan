---
title: Intake produces brief.md as a frozen handoff artifact
type: decision
created: '2026-04-24T09:30:09Z'
modified: '2026-04-24T09:30:09Z'
---

The intake phase in koan (`koan/phases/intake.py`, 3 steps: Gather / Deepen / Summarize) historically produced only a chat summary -- no artifact in the run directory -- and a legacy `koan/phases/brief_writer.py` module marked `SCOPE="legacy"` existed but was never wired into an active workflow. On 2026-04-24, Leon endorsed a design change surfaced by Claude Desktop collaboration: intake will write `brief.md` to the run directory as its handoff artifact to every downstream phase (milestone-spec, milestone-review, plan-spec, plan-review, exec-review, curation). The structure has seven sections: (1) **Initiative** -- one paragraph restating the user's task in intake's refined wording; (2) **Scope** -- in-scope and out-of-scope bullets, where out-of-scope matters more because it prevents downstream scope growth; (3) **Affected subsystems** -- concrete paths/modules with one-line descriptions, grounding downstream decomposition in real code structure; (4) **Decisions** -- numbered, with rationale and rejected alternatives, each one a constraint downstream plans must respect; (5) **Constraints** -- cross-cutting (technical, architectural, operational); (6) **Assumptions** -- stated explicitly so they are falsifiable if execution reveals them wrong; (7) **Open questions** -- caution zones for downstream phases. Lifecycle rule: `brief.md` is **frozen** after intake. If execution reveals an assumption is wrong or scope needs to shift, that event is recorded in the current milestone's Outcome section (light path) or handled by re-running intake (heavy path); silent amendment of `brief.md` is prohibited because downstream phases rely on its stability. The doctrine supersedes the legacy `brief_writer.py` approach.
